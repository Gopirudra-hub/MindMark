VITE_API_URL=http://localhost:3000


# Start all services
docker-compose up -d

# Initialize database
docker-compose exec backend npx prisma db push

# Pull AI model for local AI
docker-compose exec ollama ollama pull llama2